{
  "name": "what-should-i-read",
  "version": "1.0.0",
  "description": "express app to show book title",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "ejs": "^2.6.1",
    "express": "^4.16.3"
  },
  "scripts": {
    "start": "node index.js",
    "docker": "docker-compose stop && docker-compose rm -f && docker-compose up --build -d"
  }
}
